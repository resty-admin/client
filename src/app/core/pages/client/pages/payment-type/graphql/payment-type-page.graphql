query PaymentTypePage($orderId: String!) {
	order(id: $orderId) {
		id
		productsToOrders {
			id
			product {
				id
				price
			}
			attributesToProduct {
				id
				attribute {
					id
					price
				}
			}
			count
		}
	}
}
