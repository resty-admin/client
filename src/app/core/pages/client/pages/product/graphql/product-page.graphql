query ProductPage($productId: String!) {
	product(id: $productId) {
		id
		name
		price
		description
		file {
			id
			url
		}
		category {
			id
			name
		}
		attrsGroups {
			id
			name
			attributes {
				id
				name
				price
			}
		}
	}
}
