query Product($productId: String!) {
	product(id: $productId) {
		description
		name
		price
		id
		attrsGroups {
			name
			isUniq
			attributes {
				id
				name
			}
		}
		file {
			url
			id
		}
	}
}
