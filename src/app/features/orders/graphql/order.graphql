query Order($orderId: String!) {
	order(id: $orderId) {
		id
		orderCode
		type
		status
		totalPrice
		table {
			name
		}
	}
}

mutation CreateOrder($order: CreateOrderInput!) {
	createOrder(order: $order) {
		id
		orderCode
		status
		type
	}
}
